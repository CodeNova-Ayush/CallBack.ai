# Frontend Prototypes

This folder contains static HTML/CSS/JS prototypes and visual references.

The production frontend is the Next.js App Router code in `app/` and reusable components in `components/`.

Use this folder for reference screens only unless intentionally shipping static assets.
