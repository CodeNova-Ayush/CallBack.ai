# Components

Reusable React components live here.

## Structure

- `builder/`: resume rendering templates.
- `nav/`: application navigation.
- `ui/`: shared UI primitives.

Templates should render from structured resume data and avoid candidate-specific hardcoded content.
